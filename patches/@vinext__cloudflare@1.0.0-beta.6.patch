diff --git a/dist/cdn-warm.js b/dist/cdn-warm.js
index c5a43869b463ee91ae8b5115be9f4559fa1fe819..c27ebebd70a8d465e1a9e981fd43eaa85186191c 100644
--- a/dist/cdn-warm.js
+++ b/dist/cdn-warm.js
@@ -110,0 +111 @@ const WORKER_VERSION_OVERRIDE_HEADER = "Cloudflare-Workers-Version-Overrides";
+const WORKER_VERSION_ID_HEADER = "X-Cloudflare-Worker-Version-Id";
@@ -414,0 +416,2 @@ async function warmOnePath(target, options) {
+	let lastCacheStatus = null;
+	let lastWorkerVersionId = null;
@@ -428,0 +431,2 @@ async function warmOnePath(target, options) {
+			lastCacheStatus = response.headers.get("CF-Cache-Status");
+			lastWorkerVersionId = response.headers.get(WORKER_VERSION_ID_HEADER);
@@ -476 +482,3 @@ async function warmOnePath(target, options) {
-		retryable: lastRetryable
+		retryable: lastRetryable,
+		cacheStatus: lastCacheStatus,
+		workerVersionId: lastWorkerVersionId
@@ -536 +543,2 @@ async function warmCdnCache(options) {
-	const hasVersionOverride = new Headers(options.headers).has(WORKER_VERSION_OVERRIDE_HEADER);
+	const versionOverride = new Headers(options.headers).get(WORKER_VERSION_OVERRIDE_HEADER);
+	const hasVersionOverride = versionOverride !== null;
@@ -622,2 +630 @@ async function warmCdnCache(options) {
-		for (const failure of failures.slice(0, 5)) console.warn(`  CDN warmup failed for ${failure.path}: ${failure.error}`);
-		if (failures.length > 5) console.warn(`  CDN warmup: ${failures.length - 5} additional failure(s) omitted.`);
+		for (const { result: failure } of failedRequests) console.warn(`  CDN warmup failed for ${failure.path}: ${failure.error}; version override=${versionOverride ?? "none"}; worker version=${failure.workerVersionId ?? "missing"}; CF-Cache-Status=${failure.cacheStatus ?? "missing"}`);
